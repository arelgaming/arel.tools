# Database (Neon) — app_role (guild queries, RLS enforced)
DATABASE_URL="postgresql://app_role:password@host.neon.tech/arel?sslmode=require"
DATABASE_DIRECT_URL="postgresql://app_role:password@host.neon.tech/arel?sslmode=require"

# Database (Neon) — admin_role (admin procedures only, BYPASSRLS)
ADMIN_DATABASE_URL="postgresql://admin_role:password@host.neon.tech/arel?sslmode=require"
ADMIN_DATABASE_DIRECT_URL="postgresql://admin_role:password@host.neon.tech/arel?sslmode=require"

# Redis (Upstash)
UPSTASH_REDIS_REST_URL=""
UPSTASH_REDIS_REST_TOKEN=""

# Inngest
INNGEST_EVENT_KEY=""
INNGEST_SIGNING_KEY=""
